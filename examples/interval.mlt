(* Is correctly accepted by the typechecker. *)
let foo () = 
  let r = box (1,4) 1 in
  delay 3; 
  unbox r as y in 
  y + 1

(*
(* Correctly gets rejected by the typechecker. *)
let bar () = 
  let r = box (1,4) 1 in
  delay 5;
  unbox r as y in
  y + 1
*)

(* TODO: Is incorrectly accepted by the typechecker. *)
let fez () =
  let r = box (1,4) 1 in
  delay 3;
  let r' = r in
  delay 3;
  unbox r' as y in 
  y + 1
