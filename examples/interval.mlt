(* Typechecks as desired. *)
let foo () = 
  let r = box (1,4) 1 in
  delay 3; 
  unbox r as y in 
  y + 1

(* BUG: This version should not typecheck because [5,5] is not subinterval of [1,4]. *)
let bar () = 
  let r = box (1,4) 1 in
  delay 5;
  unbox r as y in
  y + 1

run foo ()
run bar ()