(* Run this example with the 'time-lower-bound' grading monoid, e.g.

     ./cli.exe --resources time-lower-bound examples/operations.mlt *)

operation Get : unit ~> int # 3
operation Set : int ~> unit # 7

let foo () =
  perform Get (); 
  perform Set 42; 
  let x = perform Get () in
  x + x